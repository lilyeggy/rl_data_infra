# 2026-08-28

完成 2026-08-27 SFT 评测收尾与结果固化。四组评测全部结束：MBPP holdout 为 base 40/90、SFT 68/90；HumanEval 为 base 30/164、SFT 101/164。SFT 使用 289 条 verifier 通过的 MBPP 轨迹、训练 2 epochs，最终 loss 0.1057。candidate 的失败中包含少量 ERROR 与 INSUFFICIENT_EVIDENCE，后续优先做失败分类与证据链治理，再扩大高质量训练数据并开展受控实验。

进一步分析显示，HumanEval candidate 的 63 个非通过任务中有 44 个是入口函数 `NameError`，MBPP candidate 的失败主要是断言失败（14 个）和 `NameError`（6 个）。下一步先修复 HumanEval 的任务入口与 verifier 契约并重放失败任务，再增加约 250–300 条 MBPP 高质量训练轨迹；避免用扩大数据掩盖协议问题。

定位到 serving 将生成结果硬截断为 200 tokens，已改为 32768 context window、默认 8192 output，并加入输入长度动态预算；同时将执行链路的上游异常恢复为可交给 verifier 判定。6 条重放中 2 条通过。已在 A6000 启动修复后的完整四组 holdout v2 评测，结果生成在独立的 `holdout-v2-*` 目录。
