# 2026-08-27

确认 14 个任务不足以支撑正式 SFT，第一阶段目标调整为至少 300 条 `ELIGIBLE` 轨迹。A6000 上的首批 14 个任务仍在收尾，因此提交了后台扩容编排器：等待首批完成后，按现有 14 个 SWE-bench 任务循环采样，使用 `deepseek-v4-flash`、32 次调用上限和受控工具集，累计达到 300 条有效轨迹后自动停止。

根据失败轨迹确认 SWE-bench 对当前 Teacher 偏难，改用官方 MBPP。已接入 MBPP runner/verifier，3 条 smoke 全部 `VALID / COMPLETE / PASSED / ELIGIBLE`；A6000 已启动 300 条 MBPP 正式批次，使用 16 次调用上限和独立输出目录。

MBPP 第一阶段已完成：300 条任务全部执行结束，其中 287 条 `VALID / COMPLETE / PASSED / ELIGIBLE`，准入率约 95.7%；其余 13 条被 Verifier 拒绝。批次输出位于 A6000 的 `mbpp-stage1-300-260827`。

正式 SFT 数据采集已切换到 MBPP 官方 train split，新增 ID 范围过滤并启动 `601–900` 共 300 个任务的 A6000 批次；test/validation 区间不会进入该批次。

MBPP 官方 train split 批次已完成：300 条任务全部结束，289 条 `ELIGIBLE`，Verifier 通过率约 96.3%；输出位于 A6000 的 `mbpp-train-601-900-260827`。

已完成 SFT 数据认证与打包：289 条轨迹展开为 1,163 个训练 turn，预检通过。停止 A6000 本地 14B 推理服务后启动 Qwen2.5-Coder-14B LoRA SFT，2 epochs，训练进程正在运行，输出为 `sft-runs/qwen14b-mbpp-train-260827`。

已部署训练后 watcher：训练成功并生成 adapter 后，自动启动 base 服务跑 MBPP validation `511–600` 与 HumanEval，再切换 MBPP adapter 跑同一 holdout，最终保留 candidate 服务并生成对照结果。
