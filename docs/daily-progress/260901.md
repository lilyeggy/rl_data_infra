# 2026-09-01

确认旧 MBPP SFT 的核心缺陷：原始执行证据在 `steps` 中保存了工具结果，但旧训练视图没有把 observation 插回 `messages`，因此旧 LoRA 只能作为 missing-observation ablation。现已修复 action–observation 组装并加入 fail-closed 准入，相关测试通过，修复已同步到 A6000；尚未使用正确数据重新训练。

后续工作正式收敛为三条闭环：第一，封存 MBPP、HumanEval、BigCodeBench、LiveCodeBench 等 Qwen2.5-Coder 报告 benchmark，改用公开 APPS train split 重新生产 Teacher 轨迹并完成正确 Agent SFT；第二，接入 verl/Slime，完成学生 policy rollout、认证、更新和新 policy rollout 的 Agentic RL 闭环；第三，冻结模型建立 Harness A/B、失败归因和 regression gate。SFT 数据/训练工程与 RL contract/adapter 可以并行开发，但单张 A6000 的重型任务需要分时运行，正式 RL 以正确 SFT policy-v0 为依赖。
