# 2026-08-26

今天确定项目主线为 Agentic RL 可信执行数据平台：重点建设 Agent 执行轨迹、Artifact、Verifier、Certification 和 Dataset Workflow；训练是可信数据的下游验证闭环，不把项目包装成算法创新。

完成了开发者调试控制台及 Traces、Workflows、Datasets、Artifacts、Workers 页面，整理并记录当前简历表述。核对最新代码后确认：无 GPU 的真实 Docker 执行、验证、组装和数据准入链路已经跑通；325 项功能测试可通过，当前环境另有 2 项测试因缺少 pytest 无法导入。

排查 A6000 登录时确认主机和 SSH 服务正常，问题是服务器没有接受当前 Mac 的公钥；使用密码登录后重新安装 Ed25519 公钥，免密码登录验证成功。服务器确认为 RTX A6000，14B LoRA 服务在线；远端已有多次真实 live smoke，最近三次均为 `COMPLETE / PASSED / SUCCESS`。检查时 SFT 已完成，学习循环停在 unseen DEV 评测等待阶段。

继续执行时解决了三个链路问题：模型服务漏设 immutable revision，导致有 token/logprob 却被撤销 RL 资格；Pi 子进程继承 SSH stdin，导致直接运行时 0 次模型调用后超时；watcher 只检查报告文件存在，不读取 Gate 结论。现已固化 base/candidate 模型启动脚本、为 Pi 设置 `stdin=DEVNULL`，并让 supervisor 按 `decision=ACCEPT` fail-closed 晋级。

使用同一 Pi Harness、task snapshot 和 verifier v7 完成 base 与 canonical-v2 SFT candidate 的 unseen DEV 对照。两者均为 `VALID / COMPLETE`，但都未解决任务，最终报告为 `REJECT / NO_IMPROVEMENT`；watcher 已收敛为 `REJECTED`，没有继续启动 RL candidate。当前结论是可信执行与评测闭环已在 A6000 实机跑通，但该轮 SFT 没有证明模型能力提升。

将远端 Dashboard 的模型重启入口切换到新的 candidate launcher，避免人工重启后丢失 model revision。完成本地全量回归，结果为 `349 passed, 22 subtests passed`。

将 Teacher 适配到 OpenCode Go 的 `deepseek-v4-flash`，修复 Python 请求被边缘网关返回 HTML、流式 tool call 格式不兼容，以及最终文本回复路径的状态初始化问题；同时加入模型调用上限和受控工具集合，避免 Teacher 无限探索。A6000 短 smoke 已闭环：12 次模型调用，`VALID / COMPLETE / PASSED / ELIGIBLE`。暂不启动大规模数据生产，下一步先基于该链路扩大 benchmark 任务并持续检查轨迹质量。
