# Stage C 门控复查 — 仍 BLOCKED（2026-09-09 20:50 CST）

复查方式：对 `cxr@172.17.43.193` 的只读 SSH 检查，未执行任何 GPU 命令，
未停止/修改任何进程与环境。原始快照：
`gate-check-20260909-2050CST.txt`。

## 进展（相对 18:27 检查）

- SFT 评测已全部结束：
  - MBPP：`All benchmark evaluations complete!`，sft-base-epoch1 pass@1
    为 0.796（base）/ 0.653（plus）。PID 1007953 已退出。
  - BigCodeBench：148/148 完成，Pass@1 0.00%，日志含 worker 侧
    matplotlib `ImportError ... failed to map segment from shared object`
   （评测方环境问题，与本任务无关，仅记录，未触碰）。
- P0 重新确认冻结：epoch1 adapter sha256 仍为 `6db6a40c…`，与 Phase A 一致。
- GPU1 已空闲（利用率 0%，仅 19 MiB）。

## 仍不满足的门控

- GPU0 仍被两个他人任务占用（均为非本项目工作）：
  - PID 826215（27GB）：`experiments/generate_scaling_trajectories.py
    --topup-ops ... --device cuda:0 ...`，已运行约 8.5 小时；
  - PID 2275218（15GB）：`/home/server/anaconda3/bin/python3 run_pipeline.py`，
    已运行 1 天 21 小时（注意：属 `server` 用户家目录，非本项目）。
- closeout 要求双卡共置分时（§C 固定设置：FSDP 双卡分片 + 每卡一个 vLLM 副本），
  且硬限制禁止 `pkill`/停止他人任务/清空 GPU（§10–11）。
- 用户本次只说了“可以开始”，未给出明确的双卡使用窗口
  （GPU UUID + 截止时间 + 独立运行 ID 均缺失）。

## 判定

**Stage C 仍为 BLOCKED，不得启动。** GPU 预算消耗：0 / 4 小时。

## 启动 C 所需（缺一不可）

1. 任务 owner 明确双卡使用窗口：GPU UUID
  （GPU-4288d6c9-… 与 GPU-52776180-…）、截止时间、独立运行 ID；
2. GPU0 上的 826215 / 2275218 由其 owner 自然结束或 owner 明确释放；
3. 按 `phase-c-gate/run-smoke-skeleton.sh` 填入 RUN_ID/GPU_UUIDS/DEADLINE 后执行，
   上限 60 分钟，产物标记 `framework-smoke`。
