# Stage C 门控检查报告 — BLOCKED（2026-09-09 18:27 CST）

检查方式：对 `cxr@172.17.43.193` 的只读 SSH 检查，未执行任何 GPU 命令，
未停止/修改任何进程与环境。原始快照：
`gate-check-20260909-1827CST.txt`。

## 1. 失败阶段和最小复现命令

- 失败阶段：Stage C（14B 框架环境验证）启动门控。
- 最小复现（只读，均不占 GPU）：

```bash
ssh cxr@172.17.43.193 'ps aux | grep run_official_qwen | grep -v grep'
ssh cxr@172.17.43.193 'nvidia-smi --query-compute-apps=pid,used_memory --format=csv'
ssh cxr@172.17.43.193 'tail -1 /home/cxr/agentic/rl-runs/official-qwen-base-evalplus/eval_sft_epoch1_mbpp.log'
```

## 2. 原始日志及关键错误

- MBPP 评测仍在运行：`PID 1007953`（`run_official_qwen_evalplus.py --dataset mbpp`），
  日志 `Generated 296/378 problems`，进程 `ELAPSED 55:49` 且存活。
- BigCodeBench 评测已结束（148/148），但结果为 `Passed: 0, Pass@1: 0.00%`，
  日志含 worker 侧 `ImportError: matplotlib/_c_internal_utils... failed to map
  segment from shared object`（评测方环境问题，与本任务无关，仅记录）。
- GPU 占用（`nvidia-smi`）：
  - GPU0 `GPU-4288d6c9-...`：利用率 93%，已用 76443/97887 MiB；
  - GPU1 `GPU-52776180-...`：利用率 100%，已用 27480/97887 MiB；
  - 计算进程：826215（27GB）、2275218（14GB）、1007953（34GB，MBPP 评测）、
    905956（27GB）。双卡均无容纳 14B BF16 + vLLM + FSDP 训练的空闲。

## 3. 已确认事实与尚未确认假设

已确认：

- SFT 训练已结束（epoch1 落盘，顶层 adapter 同哈希，见 Phase A manifest）。
- SFT 评测未全部结束（MBPP 296/378 进行中）。
- 双卡均被其他任务占用；即使 Unix 用户同为 `cxr` 也不代表属于本项目。
- 本次检查未获得任何 GPU 使用窗口承诺。

尚未确认（不得作为启动依据的假设）：

- MBPP 评测何时结束、结束后 GPU 是否释放、释放后窗口归属。
- 现有占卡进程（826215/2275218/905956）的归属与预计结束时间。

## 4. 已尝试的受控修复

- 无。本阻塞为资源门控而非技术故障，无需修复，只需等待。
- 未执行任何 `pkill`、环境修改、GPU 占用命令（符合 closeout 硬限制 §10–11）。

## 5. 超出本文范围的下一步建议

- 等待 MBPP 评测自然结束（按当前约 0.1 task/s，剩余约 82 题，预计约 15–25 分钟，
  仅为外推，不作为承诺）。
- 评测全部结束后，由任务 owner 明确双卡使用窗口（含 GPU UUID、截止时间、
  独立运行 ID），再按 closeout §C（60 分钟预算）启动。
- 若窗口长期不可用，可将 C 阶段拆分为“单卡最小加载探针”提案，但须先修订
  closeout 文档并获批准，不得自行降配执行。

## 6. GPU 和容器是否已经释放

- 本任务未占用任何 GPU，未启动任何容器，无需释放。
- 远端 GPU 仍被评测与其他任务占用；本次检查未做任何释放动作。
- 本任务 GPU 预算消耗：0 / 4 小时。
