# Coding VALID_FAILURE Fixture

状态：`WAITING_FOR_SERVER_CAPTURE`

这里保存一条 runtime、Harness、model backend 和 verifier 都正常工作，但 patch 未通过
任务验证的真实 Coding rollout。它是合法 reward=0 样本，不是基础设施故障。

必须保存 verifier 正常结束和 clean replay 仍 unresolved 的证据。
