# Archived local-model artifacts

The executable local-model, custom SFT, and custom GRPO prototypes were moved to
[`archive/experiments/local_model/`](../../archive/experiments/local_model/).

The remaining JSONL file is a historical experiment artifact. It is not a
certified dataset and must not be consumed by a production training entrypoint.
